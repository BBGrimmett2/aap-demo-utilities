---
controller_credentials:
  - name: Utilities SCM
    description: GitHub for Ansible SCM access
    organization: Utilities
    credential_type: Source Control
    inputs:
      username: "{{ vault_github_username }}"
      password: "{{ vault_github_token }}"
