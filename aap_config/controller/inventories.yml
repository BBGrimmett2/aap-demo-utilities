---
controller_inventories:
  - name: localhost
    description: Localhost inventory for utility playbooks
    organization: Utilities
    variables:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"

controller_hosts:
  - name: localhost
    inventory: localhost
    variables:
      ansible_connection: local
